<%- if controller_name != 'sessions' %>
  <%= button_to "Se connecter", new_session_path(resource_name), class: 'btn' %><br />
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= button_to "S'inscrire", new_registration_path(resource_name), class: 'btn btn-secondary' %><br />
<% end %>

<div style="display: flex;">
  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <%= button_to "Mot de passe oublié ?", new_password_path(resource_name), class: 'btn' %><br />
  <% end %>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= button_to "Vous n'avez pas reçu les instructions de confirmation ?", new_confirmation_path(resource_name), class: 'btn' %><br />
  <% end %>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= button_to "Vous n'avez pas reçu les instructions de déverrouillage ?", new_unlock_path(resource_name), class: 'btn' %><br />
  <% end %>
</div>
<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= button_to "Se connecter avec #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false } %><br />
  <% end %>
<% end %>
